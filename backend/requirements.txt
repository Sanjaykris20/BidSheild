# BidSheild Python Backend - Core API
# FastAPI service on port 8000

# Core web framework
fastapi[standard]>=0.110.0
uvicorn[standard]>=0.27.0

# Pydantic v2 + settings + email validation
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0

# Form / multipart parsing (UploadFile)
python-multipart>=0.0.9

# Database (async SQLAlchemy + async SQLite for dev / dev fallback)
SQLAlchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0

# Redis client (used in production; optional in dev)
redis>=5.0.0

# JWT + password hashing
python-jose[cryptography]>=3.3.0
# NOTE: passlib 1.7.x only supports bcrypt < 4.1.  Pin to 4.0.1 until
#       passlib 1.8 is released with bcrypt 5 support.
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Environment loading
python-dotenv>=1.0.0

# HTTP client (used by document router to call AI engine)
httpx>=0.27.0

# S3-compatible object storage (Cloudflare R2)
boto3>=1.34.0

# -----------------------------------------------------------------------------
# Test dependencies
# -----------------------------------------------------------------------------
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
